/**
 * Created by Administrator on 2016/8/11.
 */
var tool={
    changeAngle: function (angle) {
        return Math.PI*angle/180;
    },
    random: function () {
        return  parseInt(40+Math.random()*160);
    }
}